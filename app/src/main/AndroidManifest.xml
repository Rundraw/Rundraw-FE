<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_rundraw_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_rundraw_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RundrawFE"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <!-- 앱 시작 Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- OAuth Redirect -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="rundraw" android:host="login"/>
            </intent-filter>
        </activity>

        <!-- 회원가입 화면 -->
        <activity android:name=".SignActivity" android:exported="false" />
        <!-- 홈 화면 -->
        <activity android:name=".HomeActivity" android:exported="false" />
        <!-- 랭킹 화면 -->
        <activity android:name=".RankingActivity" android:exported="false" />
        <activity android:name=".mypage.MyPageActivity" />
        <activity android:name=".mypage.MyPageCommentsActivity" />
        <activity android:name=".mypage.MyPageCoursesActivity" />
        <activity android:name=".mypage.MyPageScrapActivity" />
        <activity android:name=".mypage.MyPageDrawnActivity" />
    </application>
</manifest>